# Development Dockerfile

FROM golang:1.25-alpine

# 安装基础依赖和 Air
RUN apk add --no-cache build-base git
RUN go install github.com/air-verse/air@v1.63.0

# 设置工作目录
WORKDIR /app

# 预先复制并下载 Go 模块，利用 Docker 缓存
COPY go.mod go.sum ./
RUN go mod download

COPY . .

CMD ["air"]